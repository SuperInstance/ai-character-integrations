[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-character-integrations"
version = "0.1.0"
description = "AI character integration examples"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
py-modules = []

[tool.pytest.ini_options]
testpaths = ["tests"]
